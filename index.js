response = window.prompt("Hello!")
if(response === "I am Ethan Hawke"){
    window-prompt("Nice to meet you!")
    if(response = string) {
        window-prompt("That's it bye.")
    }
} else {
    window-prompt("Are you Ethan Hawke?")
    if(response === "yes"){
        window-prompt("Nice to meet you!")
        if(response = string) { 
            window-prompt("That's it bye.")
        }
    } else if(response === "no"){
        window-prompt("Are you sure")
        if(response === "yes"){
            window-prompt("How do you know you're sure?")
            if(response = string) {
                window-promt("Hate to break it to you, you are Ethan Hawke.")
                if(response = string) { 
                    window-prompt("That's it bye.")
                }
            }
        } else if(response === "no"){
            window-prompt("See, you ARE Ethan Hawke!")
            if(response = string) { 
                window-prompt("That's it bye.")
            }
        } else {
            window-prompt("You're probably Ethan Hawke.")
            if(response = string) { 
                window-prompt("That's it bye.")
            }
        }
    } else {
        window-prompt("It's a yes or no question. Are you Ethan Hawke? yes or no?")
        if(response === "yes"){
            window-prompt("Nice to meet you!")
            if(response = string) { 
                window-prompt("That's it bye.")
            }
        } else if(response === "no"){
            window-prompt("Are you sure")
            if(response === "yes"){
                window-prompt("How do you know you're sure?")
                if(response = string) {
                    window-promt("Hate to break it to you, you are Ethan Hawke.")
                    if(response = string) { 
                        window-prompt("That's it bye.")
                    }
                }
            } else if(response === "no"){
                window-prompt("See, you ARE Ethan Hawke!")
                if(response = string) { 
                    window-prompt("That's it bye.")
                }
            } else {
                window-prompt("You're probably Ethan Hawke.")
                if(response = string) { 
                    window-prompt("That's it bye.")
                }
            }
        }
    }
}
